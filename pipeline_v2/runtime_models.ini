[step1]
# 当前 Step1 没有正式模型调用链，这里保留占位，方便后续统一接管
model = text_layer
provider_mode = builtin
api_key_env =
base_url_env =
reasoning_effort =

[step2]
# 这些值可被环境变量覆盖，例如：
# PIPELINE_STEP2_MODEL / PIPELINE_STEP2_PROVIDER_MODE / PIPELINE_STEP2_API_KEY_ENV / PIPELINE_STEP2_BASE_URL_ENV
# PIPELINE_STEP2_VALIDATION_FALLBACK_MODEL / PIPELINE_STEP2_VALIDATION_PROVIDER_MODE / PIPELINE_STEP2_VALIDATION_API_KEY_ENV / PIPELINE_STEP2_VALIDATION_BASE_URL_ENV
model = gpt-5.4
reasoning_effort = medium
# provider_mode: env_api_key | codex
# 默认直接走 Codex 已登录/订阅态；如需 OpenAI 兼容 key，可改回 env_api_key
provider_mode = codex
api_key_env =
base_url_env =
validation_fallback_model = gpt-5.4
validation_provider_mode = codex
validation_api_key_env =
validation_base_url_env =

[step3]
# 这些值可被环境变量覆盖，例如：
# PIPELINE_STEP3_MODEL / PIPELINE_STEP3_PROVIDER_MODE / PIPELINE_STEP3_API_KEY_ENV / PIPELINE_STEP3_BASE_URL_ENV
model = gpt-5.4
reasoning_effort = medium
# 默认直接走 Codex 已登录/订阅态；如需 OpenAI 兼容 key，可改回 env_api_key
provider_mode = codex
api_key_env =
base_url_env =

[step4]
# 这些值可被环境变量覆盖，例如：
# PIPELINE_STEP4_MODEL / PIPELINE_STEP4_PROVIDER_MODE / PIPELINE_STEP4_API_KEY_ENV / PIPELINE_STEP4_BASE_URL_ENV
model = gpt-5.4
reasoning_effort = medium
# 默认直接走 Codex 已登录/订阅态；如需 OpenAI 兼容 key，可改回 env_api_key
provider_mode = codex
api_key_env =
base_url_env =
