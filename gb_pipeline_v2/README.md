# GB Pipeline V2 官方工作区

以后所有新的国标解析-文本分析执行，都只在这个目录内完成。

## 启动方式

```bash
cd gb_pipeline_v2
python3 -m pipeline_v2 --help
```

## 目录说明

- `pipeline_v2/`：唯一正式代码入口
- `data/`：当前工作区的数据目录
- `docs/`：主线说明和整理记录
- `scripts/`：辅助脚本
- `tools/`：配套工具
- `tests/`：主线测试

## 说明

仓库根目录下遗留的历史数据和历史目录仅作保留，不再作为正式执行入口。
